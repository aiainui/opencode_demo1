version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=8000
      - SECRET_KEY=your-secret-key-change-in-production
    restart: unless-stopped
